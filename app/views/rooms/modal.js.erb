var roomModal = document.getElementById('roomModal');

roomModal.innerHTML = '<%= escape_javascript(render partial: 'shared/room_delete_modal', locals: {room: @room}) %>';

roomModal.classList.remove('c-modal-hidden');

var myModal = document.getElementById('myModal');
var modalContent = document.getElementById('modalContent');
var modalOpen = document.getElementById('modalOpen');
var cancel = document.getElementById('cancel');

modalOpen.addEventListener('click', () => {
  roomModal.classList.remove('c-modal-hidden');
});

cancel.addEventListener('click', () => {
  roomModal.classList.add('c-modal-hidden');
});

myModal.addEventListener('click', () => {
  roomModal.classList.add('c-modal-hidden');
});

modalContent.addEventListener('click', () => {
  event.stopPropagation();
});
